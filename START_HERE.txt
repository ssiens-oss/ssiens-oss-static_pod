═══════════════════════════════════════════════════════════════════════════════
  POD GATEWAY - START HERE
═══════════════════════════════════════════════════════════════════════════════

You are in: ~/ssiens-oss-static_pod

═══════════════════════════════════════════════════════════════════════════════
  STEP 1: START THE GATEWAY
═══════════════════════════════════════════════════════════════════════════════

cd gateway
export PYTHONPATH=$(pwd)
.venv/bin/python app/main.py

You should see:
✓ RunPod serverless client initialized
🧠 ComfyUI API: https://api.runpod.ai/v2/qm6ofmy96f3htl/runsync
🚀 POD Gateway starting...
🌐 Listening on 0.0.0.0:5000

═══════════════════════════════════════════════════════════════════════════════
  STEP 2: TEST IT (Open a new terminal)
═══════════════════════════════════════════════════════════════════════════════

cd ~/ssiens-oss-static_pod
./quick-test.sh

Or test manually:

curl -X POST http://localhost:5000/api/generate \
  -H "Content-Type: application/json" \
  -d '{
    "prompt": "cyberpunk cat wearing sunglasses",
    "steps": 20
  }'

═══════════════════════════════════════════════════════════════════════════════
  STEP 3: VIEW IN BROWSER
═══════════════════════════════════════════════════════════════════════════════

http://localhost:5000

Click "Generate" and enter a prompt!

═══════════════════════════════════════════════════════════════════════════════
  TROUBLESHOOTING
═══════════════════════════════════════════════════════════════════════════════

Port 5000 in use?
  pkill -f "python.*main.py"

Can't find files?
  pwd  # Make sure you're in ~/ssiens-oss-static_pod
  ls   # Should see: gateway/ test-runpod-pipeline.sh quick-test.sh

Gateway starts but 404 errors?
  That's okay! The /runsync endpoint returns results immediately.
  Images should still download. Check the terminal for:
  "Successfully downloaded X image(s)"

═══════════════════════════════════════════════════════════════════════════════
