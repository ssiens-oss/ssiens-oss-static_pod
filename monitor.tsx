import React from 'react'
import ReactDOM from 'react-dom/client'
import PodEngineMonitor from './PodEngineMonitor'
import './index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <PodEngineMonitor />
  </React.StrictMode>,
)
